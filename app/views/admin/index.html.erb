<h1>관리자페이지</h1>
<div class="row">
  <!--왼쪽 화면 -->

  <div class="col-md-2" id ="admin_left">
    <div class = "container">
      <a href="/admin/index">회원관리</a> </br> <!-- 디폴트 값은 회원관리 페이지 -->
      <a href="/admin/store">상점관리</a> </br>
      <a href="/admin/pincode">인증코드관리</a> </br>


    </div>
  </div>


  <!--오른쪽 화면 -->

  <div class="col-md-10">
    <h3>회원관리</h3>

    <div class = "container">

      <table class="table table-bordered">
        <thead>
        <tr>
          <th class="col-md-1">#</th>
          <th class="col-md-2">사용자이메일주소</th>
          <th class="col-md-2">이름</th>
          <th class="col-md-2">핸드폰번호</th>
          <th class="col-md-2">가입일</th>
          <th class="col-md-2">주점</th>

        </tr>
        </thead>

        <tbody>
          <%counter=1%>
          <% @user.each do |u|%>
            <tr>
              <td><%=counter%></td>
              <td><%=u.email%></td>
              <td><%=u.name%></td>
              <td><%=u.phone_number%></td>
              <td><%=u.created_at.strftime("(%F) %H:%M")%></td>

              <%if u.store.present?%>
                  <td><%=u.store.name%></td>
              <%else%>
                  <td>x</td>
              <%end%>
            </tr>
            <%counter+=1%>
          <% end %>
        </tbody>
      </table>
    </div>



  </div>
</div>
