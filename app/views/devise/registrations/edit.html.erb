<!--<h2>Edit <%= resource_name.to_s.humanize %></h2>-->
<h2><%=current_user.name%> 님 안녕하세요</h2>

<div class="row">
  <!--왼쪽 화면 -->

  <div class="col-md-2">
    <div class = "container">
      <a href="/home/setting">기본정보</a> </br>
      <a href="/users/edit">비밀번호 변경 / 계정삭제</a> </br>
      <a href="/home/setting_store">상점정보 업데이트</a></br>
    </div>
  </div>


  <!--오른쪽 화면 -->

  <div class="col-md-10">
    <div class = "container">
    <h3>비밀번호 변경하기</h3>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= devise_error_messages! %>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>

        <div class="field">
          <%= f.label :새로운_비밀번호 %> <i>(비밀번호를 변경하고 싶지 않으면 이 칸을 비워두세요)</i><br />
          <%= f.password_field :password, autocomplete: "off" %>
          <% if @minimum_password_length %>
              <br />
              <em><%= @minimum_password_length %> 글자 이상 입력 해주세요.</em>
          <% end %>
        </div>

        <div class="field">
          <%= f.label :새로운_비밀번호_확인 %><br />
          <%= f.password_field :password_confirmation, autocomplete: "off" %>
        </div>

        <div class="field">
          <%= f.label :이전_비밀번호를_입력해주세요 %> <i>(사용자확인을 위해 기존 비밀번호를 입력해주세요.)</i><br />
          <%= f.password_field :current_password, autocomplete: "off" %>
        </div>

        <div class="actions">
          <%= f.submit "변경완료" %>
        </div>
    <% end %>
--------------------------------------------------------------------------------------------------------
    <h3>계정 삭제하기 </h3>

    <p>계정을 삭제하면 모든 정보가 사라지게됩니다. <%= button_to "계정삭제", registration_path(resource_name), data: { confirm: "정말로 계정을 삭제 하시겠습니까?" }, method: :delete %></p>

    <%= link_to "Back", :back %>
    </div>
  </div>
</div>



















